---

- name: Provision role
  hosts: all

  pre_tasks:
    - name: Setup for tests
      become: yes
      shell: "/bin/bash /tmp/kitchen/setup.sh"

  roles:
    - role: ansible-aws-openvpn
      become: yes

